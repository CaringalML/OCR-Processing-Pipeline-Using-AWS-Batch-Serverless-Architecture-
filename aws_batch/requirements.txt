# ================================================================================================
# OCR PROCESSING PIPELINE DEPENDENCIES
# ================================================================================================
# Optimized for fast, high-quality document processing at scale

# === CORE AWS DEPENDENCIES (REQUIRED) ===
boto3>=1.34.0                   # AWS SDK for Textract, Comprehend, S3, DynamoDB
botocore>=1.34.0               # AWS core functionality
requests>=2.31.0               # HTTP requests for AWS APIs
regex>=2022.10.31              # Enhanced regex (better than built-in re)

# === TEXT PROCESSING LIBRARIES ===
# Essential libraries for quality OCR text correction
ftfy==6.1.1                    # Fix OCR encoding issues (fast, essential)
rapidfuzz==3.6.1              # Fast fuzzy string matching (OCR error correction)
pyspellchecker==0.7.2         # Lightweight spell checking

# === EMAIL/URL PROCESSING ===
# Extract and validate structured data from documents
email-validator==2.1.0        # Email validation and normalization
furl==2.1.3                   # Advanced URL parsing

# === ADVANCED PROCESSING ===
# High-quality text processing for best results:
language-tool-python==2.7.1   # Advanced grammar correction (slower but high quality)
# spacy==3.7.6                  # NLP processing (heavier but very capable)

# ================================================================================================
# PERFORMANCE CHARACTERISTICS:
# ================================================================================================
# - Container size: ~400-500MB (includes language-tool-python JVM)
# - Cold start: ~8-12 seconds (includes language tool initialization)  
# - Memory usage: ~512MB (language-tool-python requires JVM)
# - Processing time: ~5-15 seconds (higher quality grammar correction)
# - Quality: Advanced grammar correction + essential text processing
# - Scalability: Handles tens of thousands of documents efficiently
# ================================================================================================